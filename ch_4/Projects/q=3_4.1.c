#include <stdio.h>

void main()
{
    int x, y, s;

    printf("Enter the value of x: ");
    scanf("%d", &x);

    printf("Enter the value of y: ");
    scanf("%d", &y);

    s = x + y + 2;

    printf("The result of the formula s = x + y + 2 is: %d\n", s);
}
